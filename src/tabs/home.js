import '../assets/images/ice-cream.png';
export default () => {
    const wrapper = document.createElement('main');
    wrapper.classList.add('home-container');
    const hero = document.createElement('div');
    return wrapper;
}